<h2>Editing Book</h2>
<%= form_for(@book) do |f| %>
	<h5>Title</h5>
	<%= f.text_field :title %>
	<h5>Body</h5>
	<%= f.text_field :body %><br>
	<%= f.submit 'Update Book' %><br>
	<%= link_to 'show', book_path(@book.id) %>
	<%= link_to 'back', books_path %>
<% end %>